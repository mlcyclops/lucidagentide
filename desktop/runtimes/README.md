# Bundled runtimes (bun, uv) are downloaded per-OS by the build-desktop CI
# workflow into this dir and packaged via extraResources. Kept so the dir
# always exists for electron-builder even on local (unbundled) builds.
